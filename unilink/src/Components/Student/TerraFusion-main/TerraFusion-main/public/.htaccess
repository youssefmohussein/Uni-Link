RewriteEngine On
RewriteBase /TerraFusion/public/

# Send everything to index.php if not a real file/dir
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

